{% extends 'base.html.twig' %}

{% block body %}
    <h1>Familias list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Familia</th>
                <th>Acciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for familium in familias %}
            <tr>
                <td><a href="{{ path('familia_show', { 'id': familium.id }) }}">{{ familium.id }}</a></td>
                <td>{{ familium.familia }}</td>
                <td>{{ familium.acciones }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('familia_show', { 'id': familium.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('familia_edit', { 'id': familium.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('familia_new') }}">Create a new familium</a>
        </li>
    </ul>
{% endblock %}
