{% extends 'base.html.twig' %}

{% block body %}
    
    {#Muestra la barra superior#}
    {% embed "barranavegacion.html.twig" %}{% endembed %}
    <h1>Nueva venta</h1>
    <div class="container" style="margin-top: 20px;">
    {{ form_start(form) }}
        <div class="row" style="margin-bottom: 20px;">          
            <div class="col-lg-2">
                <b><i class="fa fa-calendar-check-o"></i> {{ form_label(form.fecha) }}</b>
                {{ form_widget(form.fecha) }}
            </div>
             <div class="col-lg-3">
                <b><i class="fa fa-man"></i> {{ form_label(form.cliente) }}</b>
                {{ form_widget(form.cliente) }}
            </div>  
             <div class="col-lg-3">
                <b><i class="fa fa-card"></i> {{ form_label(form.tipoPago) }}</b>
                {{ form_widget(form.tipoPago) }}
            </div>  
             <div class="col-lg-3">
                <b>{{ form_label(form.iVA) }}</b>
                {{ form_widget(form.iVA) }}
            </div>  
            <div class="col-lg-3">
                <b>{{ form_label(form.trabajador) }}</b>
                {{ form_widget(form.trabajador) }}
            </div> 
        </div>
            {{ form_widget(form) }}
        <input type="submit" value="Añadir concepto" class="btn btn-primary btn-flat"/> 
        <a href="{{ path('albaran_imprime', { 'id': albaran.id }) }}" target="_blank" class="btn btn-primary btn-flat"><i class="fa fa-print"></i></a>
        <a href="{{ path('albaran_index') }}" class="btn btn-primary btn-flat">Volver</a>
    {{ form_end(form) }}
    </div>
{% endblock %}
